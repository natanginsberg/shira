<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@android:id/background"
        android:drawable="@drawable/circle_shape" />   <!-- Your progress bar background drawable(SVG) here -->
    <item android:id="@android:id/progress">
        <clip android:drawable="@drawable/circular_progress_bar" />  <!-- Your progress bar fill drawable(SVG) here -->
    </item>
</layer-list>